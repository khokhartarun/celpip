/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: "https://celpip.globalsteps.ca", // Replace with actual domain
  generateRobotsTxt: true, // (Optional) Generate robots.txt file
  sitemapSize: 10000, // Optional, split into multiple files if too many URLs
  generateIndexSitemap: false,
};
